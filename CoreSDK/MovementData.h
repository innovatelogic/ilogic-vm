#ifndef __movementdata_h__
#define __movementdata_h__

#ifdef _WIN32
#pragma once
#endif
/*
#include "Vector.h"
#include "NxPhysics.h"

class MovementData
{
public:
	MovementData();

	void StartJump(float v0);
	void StopJump();
	float GetHeight(float elapsedTime, const NxVec3& Gravity);

	void SetSpeed(float speed) { CharacterSpeed = speed; }
	float GetSpeed() { return CharacterSpeed; }

	bool  bJump;
	float CharacterSpeed;
	float fV0;
	float fJumpTime;
	Vector vDisplace;

	// helper
	float Height;
};
*/
#endif//__movementdata_h__